**Command Handlers**

Directory contains all the command handler plugins. These command handlers will be called to execute each G&M codes.

Each command lives in its own directory.

Any changes to the command handler code needs to compile separately using makePlugins.sh script

Also any changes to datatypes needs recompilation of command handlers as well.